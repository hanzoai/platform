services:
  trilium:
    image: zadam/trilium:latest
    ports:
      - 8080
    networks:
      - hanzo-network
    restart: always
    volumes:
      - /root/trilium-backups:/home/node/trilium-data/backup
  
networks:
  hanzo-network:
    external: true
