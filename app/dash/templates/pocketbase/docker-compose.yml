version: "3.8"
services:
  pocketbase:
    image: spectado/pocketbase:0.23.3
    restart: unless-stopped
    volumes:
      - /etc/hanzo/templates/${HASH}/data:/pb_data
      - /etc/hanzo/templates/${HASH}/public:/pb_public
      - /etc/hanzo/templates/${HASH}/migrations:/pb_migrations
