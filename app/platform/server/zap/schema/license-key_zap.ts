// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: license-key.zap

import {
  buildRequest,
  parseRequest,
  buildResponse,
  parseResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

/** Method ordinals for the LicenseKey service (stable wire ids). */
export const LicenseKeyMethod = {
  activate: 1,
  validate: 2,
  deactivate: 3,
  getEnterpriseSettings: 4,
  haveValidLicenseKey: 5,
  updateEnterpriseSettings: 6,
} as const;

/** Typed RPC client for the LicenseKey service over a ZAP call channel. */
export interface LicenseKeyChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class LicenseKeyClient {
  constructor(private readonly channel: LicenseKeyChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async activate(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: LicenseKeyMethod.activate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`LicenseKey.activate: status ${resp.status}`);
    }
    return resp.body;
  }

  async validate(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: LicenseKeyMethod.validate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`LicenseKey.validate: status ${resp.status}`);
    }
    return resp.body;
  }

  async deactivate(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: LicenseKeyMethod.deactivate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`LicenseKey.deactivate: status ${resp.status}`);
    }
    return resp.body;
  }

  async getEnterpriseSettings(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: LicenseKeyMethod.getEnterpriseSettings,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`LicenseKey.getEnterpriseSettings: status ${resp.status}`);
    }
    return resp.body;
  }

  async haveValidLicenseKey(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: LicenseKeyMethod.haveValidLicenseKey,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`LicenseKey.haveValidLicenseKey: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateEnterpriseSettings(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: LicenseKeyMethod.updateEnterpriseSettings,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`LicenseKey.updateEnterpriseSettings: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the LicenseKey service. Subclass and implement each method. */
export abstract class LicenseKeyServer {
  abstract activate(args: Uint8Array): Promise<Uint8Array>;
  abstract validate(args: Uint8Array): Promise<Uint8Array>;
  abstract deactivate(args: Uint8Array): Promise<Uint8Array>;
  abstract getEnterpriseSettings(args: Uint8Array): Promise<Uint8Array>;
  abstract haveValidLicenseKey(args: Uint8Array): Promise<Uint8Array>;
  abstract updateEnterpriseSettings(args: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case LicenseKeyMethod.activate: {
        const body = await this.activate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case LicenseKeyMethod.validate: {
        const body = await this.validate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case LicenseKeyMethod.deactivate: {
        const body = await this.deactivate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case LicenseKeyMethod.getEnterpriseSettings: {
        const body = await this.getEnterpriseSettings(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case LicenseKeyMethod.haveValidLicenseKey: {
        const body = await this.haveValidLicenseKey(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case LicenseKeyMethod.updateEnterpriseSettings: {
        const body = await this.updateEnterpriseSettings(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


