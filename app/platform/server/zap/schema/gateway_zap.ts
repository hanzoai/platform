// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: gateway.zap

import {
  buildRequest,
  parseRequest,
  buildResponse,
  parseResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

/** Method ordinals for the Gateway service (stable wire ids). */
export const GatewayMethod = {
  status: 1,
  listRateLimits: 2,
  createRateLimit: 3,
  updateRateLimit: 4,
  deleteRateLimit: 5,
  listRoutes: 6,
  createRoute: 7,
  updateRoute: 8,
  deleteRoute: 9,
} as const;

/** Typed RPC client for the Gateway service over a ZAP call channel. */
export interface GatewayChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class GatewayClient {
  constructor(private readonly channel: GatewayChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async status(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.status,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.status: status ${resp.status}`);
    }
    return resp.body;
  }

  async listRateLimits(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.listRateLimits,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.listRateLimits: status ${resp.status}`);
    }
    return resp.body;
  }

  async createRateLimit(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.createRateLimit,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.createRateLimit: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateRateLimit(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.updateRateLimit,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.updateRateLimit: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteRateLimit(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.deleteRateLimit,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.deleteRateLimit: status ${resp.status}`);
    }
    return resp.body;
  }

  async listRoutes(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.listRoutes,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.listRoutes: status ${resp.status}`);
    }
    return resp.body;
  }

  async createRoute(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.createRoute,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.createRoute: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateRoute(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.updateRoute,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.updateRoute: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteRoute(args: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: GatewayMethod.deleteRoute,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: args,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Gateway.deleteRoute: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the Gateway service. Subclass and implement each method. */
export abstract class GatewayServer {
  abstract status(args: Uint8Array): Promise<Uint8Array>;
  abstract listRateLimits(args: Uint8Array): Promise<Uint8Array>;
  abstract createRateLimit(args: Uint8Array): Promise<Uint8Array>;
  abstract updateRateLimit(args: Uint8Array): Promise<Uint8Array>;
  abstract deleteRateLimit(args: Uint8Array): Promise<Uint8Array>;
  abstract listRoutes(args: Uint8Array): Promise<Uint8Array>;
  abstract createRoute(args: Uint8Array): Promise<Uint8Array>;
  abstract updateRoute(args: Uint8Array): Promise<Uint8Array>;
  abstract deleteRoute(args: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case GatewayMethod.status: {
        const body = await this.status(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.listRateLimits: {
        const body = await this.listRateLimits(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.createRateLimit: {
        const body = await this.createRateLimit(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.updateRateLimit: {
        const body = await this.updateRateLimit(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.deleteRateLimit: {
        const body = await this.deleteRateLimit(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.listRoutes: {
        const body = await this.listRoutes(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.createRoute: {
        const body = await this.createRoute(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.updateRoute: {
        const body = await this.updateRoute(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case GatewayMethod.deleteRoute: {
        const body = await this.deleteRoute(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


