// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: result.zap

import {
  Builder,
  Message,
  StructView,
} from "@zap-proto/zap";

const resultStructOff = {
  value: 0,
} as const;
const resultStructSize = 8;

/** Zero-copy view into a ZAP-encoded ResultStruct message. */
export class ResultStruct extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): ResultStruct {
    const root = Message.parse(b).root();
    return new ResultStruct(root.data, root.offset);
  }

  get value(): string { return this.text(resultStructOff.value); }
}

/** Field values for newResultStruct. */
export interface ResultStructInput {
  value: string;
}

/** Build a ZAP-encoded ResultStruct message from `in` and return the bytes. */
export function newResultStruct(in_: ResultStructInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(resultStructSize);
  ob.setText(resultStructOff.value, in_.value);
  ob.finishAsRoot();
  return b.finish();
}


