# Platform Frontend-Only Mode
# Usage: pnpm dev:frontend
# No Express server, no databases — just the UI proxied to production APIs.

SKIP_ENV_VALIDATION=1
NODE_ENV=development

# Override if you want to proxy to a different platform backend
# PLATFORM_API_URL=https://platform.hanzo.ai
