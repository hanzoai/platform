{
	"name": "hanzo",
	"private": true,
	"workspaces": ["app/*", "pkg/*"],
	"scripts": {
		"hanzo:setup": "pnpm --filter=hanzo run setup",
		"hanzo:dev": "pnpm --filter=hanzo run dev",
		"hanzo:dev:turbopack": "pnpm --filter=hanzo run dev-turbopack",
		"hanzo:build": "pnpm --filter=hanzo run build",
		"hanzo:start": "pnpm --filter=hanzo run start",
		"test": "pnpm --filter=hanzo run test",
		"server:script": "pnpm --filter=server run switch:dev",
		"server:dev": "pnpm --filter=server run dev",
		"server:build": "pnpm --filter=server run build",
		"docker:build:canary": "./app/hanzo/docker/build.sh canary",
		"typecheck": "pnpm -r run typecheck",
		"build": "pnpm -r run build",
		"format-and-lint": "biome check .",
		"check": "biome check --write --no-errors-on-unmatched --files-ignore-unknown=true",
		"format-and-lint:fix": "biome check . --write"
	},
	"devDependencies": {
		"dotenv": "16.4.5",
		"esbuild": "0.20.2",
		"tsx": "4.16.2",
		"lint-staged": "^15.2.7",
		"@biomejs/biome": "1.9.4",
		"lefthook": "1.8.4",
		"@commitlint/cli": "^19.3.0",
		"@commitlint/config-conventional": "^19.2.2",
		"@types/node": "^18.17.0"
	},
	"packageManager": "pnpm@9.5.0",
	"engines": {
		"node": "^20.9.0",
		"pnpm": ">=9.5.0"
	},
	"lint-staged": {
		"*": [
			"biome check --write --no-errors-on-unmatched --files-ignore-unknown=true"
		]
	},
	"commitlint": {
		"extends": ["@commitlint/config-conventional"]
	},
	"resolutions": {
		"@types/react": "18.3.5",
		"@types/react-dom": "18.3.0"
	}
}
